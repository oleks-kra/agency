extends base

mixin oneArticle(article)
  li
    p=article.title
    - var decodedArticleHtml = decodeURIComponent(article.content)
    p !{decodedArticleHtml}


block content 
  h1 Welcome
  ul
    each article in articles
      +oneArticle(article)
    